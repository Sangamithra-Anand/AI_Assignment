# 📈 Time Series Forecasting of Exchange Rates

This project performs **time-series forecasting** on historical USD → AUD exchange rates using:

- **ARIMA (AutoRegressive Integrated Moving Average)**
- **Exponential Smoothing (Simple, Holt)**

It includes a complete data-processing pipeline with:
✔ Automatic folder creation  
✔ Data loading & preprocessing  
✔ Visualization (Time Series, ACF, PACF)  
✔ Model training & forecasting  
✔ Evaluation (MAE, RMSE, MAPE)  
✔ Plots & report generation  

---

## 🚀 Project Structure

```
Time_series/
│
├── data/
│   └── exchange_rate.csv
│
├── src/
│   ├── main.py
│   ├── load_data.py
│   ├── preprocess.py
│   ├── arima_model.py
│   ├── exp_smoothing.py
│   ├── visualize.py
│   ├── evaluate.py
│   └── utils.py
│
├── models/
│   ├── arima_model.pkl
│   └── exp_smoothing.pkl
│
├── results/
│   ├── plots/
│   ├── metrics/
│   └── forecasts/
│
├── requirements.txt
└── README.md
```

---

## 📦 Installation

Install all required dependencies:

```bash
pip install -r requirements.txt
```

---

## ▶️ How to Run the Project

Execute the main script:

```bash
python src/main.py
```

The script will automatically:

- Create required folders
- Load & clean the dataset
- Generate plots (Time Series, ACF, PACF)
- Train ARIMA & Exponential Smoothing models
- Produce 30-day forecasts
- Evaluate accuracy
- Save all results into the **results/** directory

---

## 🧼 Data Preprocessing

Preprocessing steps include:

- Converting `date` to datetime
- Sorting by chronological order
- Removing missing values (forward/backward fill)
- Removing extreme anomalies (3σ clipping)

---

## 📊 Visualizations Generated

The system automatically generates:

| Plot | Description |
|------|-------------|
| **time_series.png** | Raw historical USD→AUD exchange rate trend |
| **acf.png** | Autocorrelation plot (helps determine ARIMA `q`) |
| **pacf.png** | Partial autocorrelation plot (helps determine ARIMA `p`) |
| **arima_forecast.png** | Forecast generated by ARIMA |
| **exp_smoothing_forecast.png** | Forecast generated by Exponential Smoothing |

All outputs are saved in:

```
results/plots/
```

---

## 🤖 Models Used

### 1️⃣ **ARIMA Model**
- Captures autoregressive + moving average components
- Suitable for stable financial time-series
- Produces multi-step forecasts

Model file saved at:

```
models/arima_model.pkl
```

### 2️⃣ **Exponential Smoothing**
- Simple smoothing: no trend  
- Holt method: handles trend  
- (Holt-Winters available for seasonality)

Model file saved at:

```
models/exp_smoothing.pkl
```

---

## 📈 Forecast Results

Forecast CSV files are stored in:

```
results/forecasts/
```

- `arima_forecast_values.csv`
- `exp_smoothing_values.csv`

Each contains:

| Date | Forecast |
|------|----------|

---

## 🧮 Evaluation Metrics

The evaluation system computes:

- **MAE** – Mean Absolute Error  
- **RMSE** – Root Mean Squared Error  
- **MAPE (%)** – Mean Absolute Percentage Error  

Evaluation reports saved at:

```
results/metrics/arima_eval.json
results/metrics/exp_eval.json
```

---

## 🧠 Key Learnings

- How to preprocess and analyze time-series data  
- How ACF & PACF help determine ARIMA parameters  
- How smoothing methods apply to financial forecasting  
- How to evaluate forecasting models  
- How to design a modular ML project  

---

## 🛠 Technologies Used

- Python  
- Pandas  
- NumPy  
- Statsmodels  
- Matplotlib  
- Joblib  

---

## 📌 Future Improvements

- Add **Auto ARIMA** for model selection  
- Add **Holt-Winters seasonal forecasting**  
- Add **hyperparameter tuning (grid search)**  
- Build a **Streamlit dashboard** for interactive forecasting  
- Add **error bars & confidence intervals**  

---

## 📄 License  
This project is free to use for learning and academic purposes.

---

**Happy Coding! ✨**